{% extends 'base.html.twig' %}

{% block title %}Unités{% endblock %}

{% block body %}
    <h1>Unités</h1>

    <div class="container">
        {% for baie in baies %}
            <div class="card" style="width: 30rem;">
                <div class="card-header">
                    Baie N°{{ baie.id }} / Statut : {% if baie.status == 0 %}
                        complete {% else %}
                        libre
                    {% endif %}
                    / Nombre d'Unités : {{ baie.NbrEmplacement }}
                </div>
                <div class="card-body">
                    {% for unite in unites %}
                        <blockquote class="blockquote mb-{{ unite.numero }}">
                            <dt>Unité N°{{ unite.numero }}</dt>
                            <dt>Status</dt>
                            {% if unite.status == 0 %}
                                <dd>complete</dd>
                            {% else %}
                                <dd>libre</dd>
                            {% endif %}
                        </blockquote>
                    {% endfor %}
                    <a href="{{ url('app_unite_details') }}" class="btn btn-primary">Détails</a>
                </div>
            </div>
        {% endfor %}
    </div>
<a class="btn btn-success" href="{{ url('app_unite_new') }}">Ajouter</a>

    {#    <dl> #}
    {#        {% for unite in unites %} #}
    {#            <dt>Id</dt> #}
    {#            <dd>{{ unite.id }}</dd> #}
    {#            <dt>Status</dt> #}
    {#            {% if unite.status == 0 %} #}
    {#                <dd>complete</dd> #}
    {#            {% else %} #}
    {#                <dd>libre</dd> #}
    {#            {% endif %} #}
    {#            #}{#            <dt>Type</dt> #}
    {#            #}{#            <dd>{{ unite.gettype_unite() }}</dd> #}
    {#        {% endfor %} #}
    {#    </dl> #}
{% endblock %}
